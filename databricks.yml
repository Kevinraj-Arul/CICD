# Databricks Asset Bundle configuration
# https://docs.databricks.com/en/dev-tools/bundles/settings.html

bundle:
  name: "CICD"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: production

    workspace:
      host: https://dbc-3c33e389-a9f2.cloud.databricks.com/
      # We always use a fixed root path to avoid duplicate resources
      root_path: /Users/kevinraj.arul@diggibyte.com/.bundle

    # Run bundle operations as this user
    run_as:
      user_name: "kevinraj.arul@diggibyte.com"
